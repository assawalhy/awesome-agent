# TODO

- [x] `skills/awesome-plan/SKILL.md`: add a "Challenge the idea" concept (respectful
      pushback, name assumptions/risks) and extend the Research step: "do the research,
      don't ask what you can find yourself; before finalizing a plan that shifts from the
      original ask for tradeoff/scope reasons, name the shift and ask the user to confirm."
      Also strengthen Execute step: mark `[x]` per item as soon as verified — update
      TODO.md as you go, never batch at the end.
- [x] `agents/awesome-agent.md`: extend non-negotiables with challenge, research-before-asking,
      ask-before-shifting-the-ask lines, and update-TODO-as-you-go (mark `[x]` immediately).
- [x] `harnesses/claude/agents/awesome-agent.md`: extend Core rules with the same three behaviors.
- [x] `harnesses/cursor/agents/awesome-agent.md`: mirror the claude edits.
- [x] `harnesses/kiro/agents/awesome-agent.md`: extend the prompt-body Rules with the same
      three behaviors (body = plain markdown, no YAML concerns).
- [x] `harnesses/kiro/agents/awesome-agent.json`: apply the identical prompt-body edit
      (JSON `prompt`).
- [x] `commands/epic.md`: extend the interview step — challenge the goal, research before
      asking, confirm any shift from the original ask before planning.
- [x] README.md: add a line noting the challenge/research/ask-before-shifting behavior.
- [x] Copy changed shared files into `.awesome-agent/` mirror (skill + agent).
- [x] Verify: JSON parses; kiro md prompt body == json prompt; every source + mirror +
      installed copy contains the new challenge wording.
- [x] Reinstall to opencode, claude, kiro (and cursor if configured); verify installed
      copies contain the new wording.