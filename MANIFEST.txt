# awesome-agent file manifest
# format: <repo-relative-path>|<version_added>|<version_removed>
# version_removed is "-" while the file is still shipped.
# When a file is removed from the plugin, set version_removed to the release
# that dropped it (e.g. 0.2.0). Uninstall/update then still clean it up from
# every target it was ever installed to, even though it no longer exists here.
#
# Per-harness overlay files live under harnesses/<id>/ and are resolved at
# install time: a harness-specific file wins over the same-named shared file,
# and harnesses/<id>/.skip excludes shared files for that harness. Overlays are
# tracked here by their logical path (e.g. agents/awesome-agent.md), so the
# entries below are the only installable paths.

commands/todo.md|0.1.0|-
commands/continue.md|0.1.0|-
commands/epic.md|0.1.0|-
commands/go.md|0.1.0|-
agents/awesome-agent.md|0.1.0|-
agents/awesome-agent.json|0.4.0|-
skills/awesome-plan/SKILL.md|0.1.0|-
skills/pr-description/SKILL.md|0.4.0|-
